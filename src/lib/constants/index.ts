// Barrel export for all constants
export { metadata } from "./metadata";
export { header } from "./navigation";
export { intro, contact, footer, socials } from "./personal-info";
export { chatbot } from "./chatbot-config";

// Re-export legacy paths for backward compatibility (can be removed gradually)
export { metadata as metadataContent } from "./metadata";
